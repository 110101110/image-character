# Image & Character

A custom ascii art generator that fits my need!

*to do list:*
-	possible glith effet: remplace certains characters by random character from an input
-	refactor utf8 related fucntions (e.g. get cursor), one cpp file for text edit related function, one cpp file for gif generation
- create fucntions for each image processus

*to use*
- first download the repo and launch your terminal inside the folder

- second, compile with the provided Makefile
	```bash
	make
	```

	*if not sure having all the dependecies, use this line instead:*
	```bash
	make install-deps
	```

- lastly, launch the executable
	```bash
	./image_character
	```

<!-- key features of the program -->

<!-- what i can propose through this project, associated with low level programming and computer vision (image processing) -->

<!-- utf8 encoding and the difference with Ascii -->

<!-- image processing: brightness, contrast, color inverse, thresholding, binary mask, boundary outline, sobel operator -->

<!-- before and after processing examples in image and the final ascii output -->

